---
title: Boolean Parsons
sidebar_position: 1        # optional: shows up in the docs sidebar
---

import Head from '@docusaurus/Head';

<Head>
  <link rel="stylesheet" href="/parsons/parsons.css" />
  <link rel="stylesheet" href="/parsons/lib/prettify.css" />

  <script src="/parsons/lib/jquery.min.js"></script>
  <script src="/parsons/lib/jquery-ui.min.js"></script>
  <script src="/parsons/lib/jquery.ui.touch-punch.min.js"></script>
  <script src="/parsons/lib/underscore-min.js"></script>
  <script src="/parsons/lib/lis.js"></script>
  <script src="/parsons/lib/prettify.js"></script>
  <script src="/parsons/parsons.js"></script>
</Head>

<h2>Simple js-Parsons example assignment</h2>

<p>
  Drag and reorder the lines so the function returns <code>true</code> for a
  <code>true</code> input and <code>false</code> otherwise.
</p>

<div id="sortableTrash" class="sortable-code"></div>
<div id="sortable" class="sortable-code"></div>
<div style={{clear: 'both'}} />

<p>
  <a href="#" id="newInstanceLink">New instance</a> •
  <a href="#" id="feedbackLink">Get feedback</a>
</p>

<script>{`
const initial =
  'def is_true(boolean_value):\\n' +
  '  if boolean_value:\\n' +
  '    return True\\n' +
  '  return False\\n' +
  '  return true #distractor\\n';

function displayErrors(fb) {
  if (fb.errors.length > 0) alert(fb.errors[0]);
}

$(function () {
  const parson = new ParsonsWidget({
    sortableId: 'sortable',
    trashId: 'sortableTrash',
    max_wrong_lines: 1,
    feedback_cb: displayErrors,
  });
  parson.init(initial);
  parson.shuffleLines();
  $('#newInstanceLink').on('click', (e) => {
    e.preventDefault();
    parson.shuffleLines();
  });
  $('#feedbackLink').on('click', (e) => {
    e.preventDefault();
    parson.getFeedback();
  });
});
`}</script>
